# This is expected because `release_delay` is not set to true.
entity_state_attributes:
  brightness: 50
entity_state: "on"
fired_actions: [brightness_up_hold, brightness_up_release]
expected_calls_count: 9
