example_app:
  module: controllerx
  class: SwitchController
  controller: my_controller
  integration:
    name: z2m
    listen_to: mqtt
  switch:
    name: switch.my_switch
  mapping:
    toggle: toggle
